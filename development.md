# add new selection and config file
* update: `src/config.js`.
* add new eslint config file in `src/configFiles`.
* update `package.json` version.
# Notice
* Files in this path `src/configFiles`. Will auto replace the suffix `js` with `cjs`.
